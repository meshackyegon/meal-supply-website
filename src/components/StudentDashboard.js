// // src/components/StudentDashboard.js

// import React from 'react';

// const StudentDashboard = () => {
//   return (
//     // Your student dashboard JSX with charts
//   );
// };

// export default StudentDashboard;

// src/components/StudentDashboard.js

import React from 'react';

const StudentDashboard = () => {
  // Fetch student data and transactions from the backend
  // Use a chart library (e.g., Chart.js) to display monetary records

  return (
    <div>
      <h2>Student Dashboard</h2>
      {/* Display charts and other relevant information */}
    </div>
  );
};

export default StudentDashboard;

